apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: funhouse-room-room6
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`riddler.rycr.io`) && PathPrefix(`/r/SQsV9Wi0iHpB`)
      kind: Rule
      services:
        - name: funhouse-room-room6
          port: 80
      middlewares:
        - name: funhouse-room-room6-strip
  # Use Traefik entrypoint / default cert store (TLS secret stays in app ns).
  tls: {}
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: funhouse-room-room6-strip
spec:
  stripPrefix:
    prefixes:
      - /r/SQsV9Wi0iHpB
